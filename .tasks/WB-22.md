Title: append_evidence MCP tool
Description: Implement the `append_evidence` MCP tool. Accumulates evidence entries on a task without changing its status. Args: task_id, entry.

Acceptance Criteria:
- `resources/skills/workbench-emulator/SKILL.md` updated to guide the emulator to prefer calling `append_evidence` MCP tool over directly appending to the task file Evidence section, with file-based fallback when unavailable.
- 100% test coverage maintained (`npm run test:coverage` passes).
- `append_evidence` tool is documented in the MCP server reference.
