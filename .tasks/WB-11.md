Title: route_for_review MCP tool
Description: Implement the `route_for_review` MCP tool. Transitions a task from `implemented` → `review_required`. Args: task_id.

Acceptance Criteria:
- `resources/skills/workbench-emulator/SKILL.md` updated to guide the emulator to prefer calling `route_for_review` MCP tool over directly updating task file YAML, with file-based fallback when unavailable.
- 100% test coverage maintained (`npm run test:coverage` passes).
- `route_for_review` tool is documented in the MCP server reference.
