Title: verify_task MCP tool
Description: Implement the `verify_task` MCP tool. Transitions a task from `review_required` → `verified`. Args: task_id.

Acceptance Criteria:
- `resources/skills/workbench-emulator/SKILL.md` updated to guide the emulator to prefer calling `verify_task` MCP tool over directly updating task file YAML, with file-based fallback when unavailable.
- 100% test coverage maintained (`npm run test:coverage` passes).
- `verify_task` tool is documented in the MCP server reference.
