Title: Emulator Phase 3 ledger seeding
Description: Update the workbench-emulator skill so Phase 3 (Task Planning) seeds the in-memory ledger by calling the `create_task` MCP tool for each planned task immediately after writing its task file. This unblocks Phase 4 and Phase 5 from using MCP tools (`claim_task`, `start_task`, `submit_task`, `route_for_review`) for all transitions instead of falling back to direct YAML file edits.

Status: done

Acceptance Criteria:
- `resources/skills/workbench-emulator/SKILL.md` Phase 3 instructs the emulator to call `create_task` MCP tool for each task after writing its task file, with file-only fallback when the tool is unavailable.
- `resources/skills/workbench-emulator/SKILL.md` and `.claude/skills/workbench-emulator/SKILL.md` are kept in sync (identical content).
- End-to-end emulator run uses MCP for claim, start, submit, and route_for_review transitions without falling back to file edits (assuming MCP server is available).
