Title: submit_task MCP tool
Description: Implement the `submit_task` MCP tool. Transitions a task from `in_progress` → `implemented`. Args: task_id, output, evidence.

Acceptance Criteria:
- `resources/skills/workbench-emulator/SKILL.md` updated to guide the emulator to prefer calling `submit_task` MCP tool over directly updating task file YAML and output sections, with file-based fallback when unavailable.
- 100% test coverage maintained (`npm run test:coverage` passes).
- `submit_task` tool is documented in the MCP server reference.
