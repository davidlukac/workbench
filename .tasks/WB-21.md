Title: retry_task MCP tool
Description: Implement the `retry_task` MCP tool. Manually re-queues an exhausted/failed task back to `pending`. Args: task_id.

Acceptance Criteria:
- `resources/skills/workbench-emulator/SKILL.md` updated to guide the emulator to prefer calling `retry_task` MCP tool over directly resetting task file YAML, with file-based fallback when unavailable.
- 100% test coverage maintained (`npm run test:coverage` passes).
- `retry_task` tool is documented in the MCP server reference.
