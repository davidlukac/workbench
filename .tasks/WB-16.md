Title: sign_off_task MCP tool
Description: Implement the `sign_off_task` MCP tool. Transitions a task from `ready_for_signoff` → `signed_off`. Args: task_id.

Acceptance Criteria:
- `resources/skills/workbench-emulator/SKILL.md` updated to guide the emulator to prefer calling `sign_off_task` MCP tool over directly updating task file YAML, with file-based fallback when unavailable.
- 100% test coverage maintained (`npm run test:coverage` passes).
- `sign_off_task` tool is documented in the MCP server reference.
