Status: in_progress
Title: CLI MCP tool
Description:

Add a CLI command `tool` that will allow user to interact with the MCP server directly from the command line (see basic memory for inspiration).

Acceptance Criteria:
- [ ] all tools and resources are listed when running `workbench tool`
- [ ] the CLI interface performs exactly the same logic as direct MCP calls without duplication of code
- [ ] user can provide arguments for the tool (or resource if applicable) either as args or paths
- [ ] test coverage and linting passes
- [ ] memory and documentation is updated

Open Questions: 
- [ ] do wee need to create a Commander Command for every tool and resource, or can we generate it from the tools metadata/definition dynamically?
