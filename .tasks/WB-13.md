Title: queue_for_signoff MCP tool
Description: Implement the `queue_for_signoff` MCP tool. Transitions a task from `verified` → `ready_for_signoff`. Args: task_id.

Acceptance Criteria:
- `resources/skills/workbench-emulator/SKILL.md` updated to guide the emulator to prefer calling `queue_for_signoff` MCP tool over directly updating task file YAML, with file-based fallback when unavailable.
- 100% test coverage maintained (`npm run test:coverage` passes).
- `queue_for_signoff` tool is documented in the MCP server reference.
