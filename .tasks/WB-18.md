Title: fail_task MCP tool
Description: Implement the `fail_task` MCP tool. Transitions a task from `in_progress` → `failed`. Args: task_id, error.

Acceptance Criteria:
- `resources/skills/workbench-emulator/SKILL.md` updated to guide the emulator to prefer calling `fail_task` MCP tool over directly updating task file YAML, with file-based fallback when unavailable.
- 100% test coverage maintained (`npm run test:coverage` passes).
- `fail_task` tool is documented in the MCP server reference.
