Title: request_changes MCP tool
Description: Implement the `request_changes` MCP tool. Transitions a task from `review_required` or `ready_for_signoff` → `changes_requested`. Args: task_id, notes.

Acceptance Criteria:
- `resources/skills/workbench-emulator/SKILL.md` updated to guide the emulator to prefer calling `request_changes` MCP tool over directly updating task file YAML and review notes, with file-based fallback when unavailable.
- 100% test coverage maintained (`npm run test:coverage` passes).
- `request_changes` tool is documented in the MCP server reference.
