Title: sign_off_tasks MCP tool
Description: Implement the `sign_off_tasks` MCP tool. Batch sign-off of multiple tasks. Args: task_ids[] or { all: true }.

Acceptance Criteria:
- `resources/skills/workbench-emulator/SKILL.md` updated to guide the emulator to prefer calling `sign_off_tasks` MCP tool for batch sign-off over iterating task files directly, with file-based fallback when unavailable.
- 100% test coverage maintained (`npm run test:coverage` passes).
- `sign_off_tasks` tool is documented in the MCP server reference.
