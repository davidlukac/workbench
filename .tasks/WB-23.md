Title: get_task_prompt MCP tool
Description: Implement the `get_task_prompt` MCP tool. Returns a fully resolved context pack for an agent: persona system prompt, task details, dependency statuses, AC items, constraints, evidence, review notes, allowed/disallowed actions, Definition of Done, and skill invocations. Args: task_id.

Acceptance Criteria:
- `resources/skills/workbench-emulator/SKILL.md` updated to guide the emulator to prefer calling `get_task_prompt` MCP tool to load task context over assembling context manually from task/spec/persona files, with file-based fallback when unavailable.
- 100% test coverage maintained (`npm run test:coverage` passes).
- `get_task_prompt` tool is documented in the MCP server reference.
