Title: update_tasks MCP tool
Description: Implement the `update_tasks` MCP tool. Validates and persists partial task draft updates. Returns updated[], rejected[], current task draft, revision, per-field results, and completeness { complete, missing[], invalid[] }. Args: base_revision?, tasks/fields.

Acceptance Criteria:
- `resources/skills/workbench-emulator/SKILL.md` updated to guide the emulator to prefer calling `update_tasks` MCP tool over directly writing task draft files during Phase 3 planning, with file-based fallback when unavailable.
- 100% test coverage maintained (`npm run test:coverage` passes).
- `update_tasks` tool is documented in the MCP server reference.
